This was supposed to be a minimal repro for a Puppeteer bug when using Firefox ≥151, in which "Node is either not clickable or not an Element" is thrown when trying to call click() on an ElementHandle of a button inside an iframe. Currently, the problem isn't reproduced, though.
